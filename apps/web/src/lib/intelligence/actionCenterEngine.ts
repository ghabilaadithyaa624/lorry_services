/**
 * Operational Action Center — web entry point.
 * Logic lives in `@lorrycarry/shared`; re-exported here for existing imports.
 */
export { deriveOperationalTasks } from '@lorrycarry/shared'
export type { OperationalTask } from '@lorrycarry/shared'
